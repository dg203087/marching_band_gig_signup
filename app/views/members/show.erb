<!DOCTYPE html>
<html>
    <body>
        <h2>LGBAC Member Profile</h2>
        <p><b>Name: </b><%= @member_obj.full_name %><br/>
        <b>Email: </b><%= @member_obj.email %><br/>
        <b>Instrument: </b><%= @member_obj.instrument %></p>

        <h3><u> Current Gig Attendance (Select to Edit/Delete)</u></h3>

        <% @gigs.each do |gig| %>
            <p><b><a href="/gigs/<%=gig.id%>"><%= gig.gig_name %></a></b><br>
            <b>Attending? </b><%= bool_to_string(gig.attending) %></p>
        <% end %>
        
        <br/>
        <a class="btn btn-primary" href="/gigs/new" role="button">Add Gig</a>
        <a class="btn btn-primary" href="/logout" role="button">Log Out</a>
    </body>
</html>
